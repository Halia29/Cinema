import { BoldPurple14pxDirective } from './bold-purple14px.directive';

describe('BoldPurple14pxDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldPurple14pxDirective();
    expect(directive).toBeTruthy();
  });
});
