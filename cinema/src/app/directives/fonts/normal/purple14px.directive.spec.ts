import { Purple14pxDirective } from './purple14px.directive';

describe('Purple14pxDirective', () => {
  it('should create an instance', () => {
    const directive = new Purple14pxDirective();
    expect(directive).toBeTruthy();
  });
});
