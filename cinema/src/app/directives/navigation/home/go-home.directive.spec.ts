import { GoHomeDirective } from './go-home.directive';

describe('GoHomeDirective', () => {
  it('should create an instance', () => {
    const directive = new GoHomeDirective();
    expect(directive).toBeTruthy();
  });
});
