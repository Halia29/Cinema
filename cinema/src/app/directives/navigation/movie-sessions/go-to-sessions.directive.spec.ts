import { GoToSessionsDirective } from './go-to-sessions.directive';

describe('GoToSessionsDirective', () => {
  it('should create an instance', () => {
    const directive = new GoToSessionsDirective();
    expect(directive).toBeTruthy();
  });
});
