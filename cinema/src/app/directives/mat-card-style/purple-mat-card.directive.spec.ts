import { PurpleShadowDirective } from './purple-mat-card.directive';

describe('PurpleMatCardDirective', () => {
  it('should create an instance', () => {
    const directive = new PurpleShadowDirective();
    expect(directive).toBeTruthy();
  });
});
