import { OrderSeatsByRowsPipe } from './order-seats-by-rows.pipe';

describe('OrderSeatsByRowsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderSeatsByRowsPipe();
    expect(pipe).toBeTruthy();
  });
});
