import { FilterBySeatPipe } from './filter-by-seat.pipe';

describe('FilterBySeatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySeatPipe();
    expect(pipe).toBeTruthy();
  });
});
